class LoanService {
  getLoanInterestInfo(loanType: string): void {
    if (loanType === "HomeLoan") {
    }
    if (loanType === "PersonalLoan") {
    }
    if (loanType === "CarLoan") {
    }
  }
}
