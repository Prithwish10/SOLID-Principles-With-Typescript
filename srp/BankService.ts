class BankService {
    withdraw(amount: number, account_number: string): void {
        // Write logic to withdraw amount from the bank
    }

    deposit(amount: number, account_number: string): void {
        // Write logic to deposit amount from the bank
    }
}