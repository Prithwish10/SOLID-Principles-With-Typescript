import { NotificationService } from "./NotificationService";

class MobileNotificationService implements NotificationService {
    sendOTP(medium: string): void {
        // Write logic to send OTP via mobile
    }
}